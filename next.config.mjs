/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['us-east-1-shared-usea1-02.graphassets.com', 'lh3.googleusercontent.com'], // Replace with your actual image domain
      },
};

export default nextConfig;
